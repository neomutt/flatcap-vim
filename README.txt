In the .vim files, the symbol names are sorted and generally wrapped after 10
symbols.

col_*.vim	Colours
com_*.vim	Commands
fn_*.vim	Functions
hooks.*		Hooks
sort_*.txt	Sorts, Don't seem to appear in the syntax file
var_*.vim	Variables
vim_*.vim	Syntax file constants

symbol/*	Raw data for *.vim
expand/*	Attempt to trace inheritance of syntax

expand/relationships.txt
		Attempt to put syntax keywords in the same order every time

everything else
		Random unfinished works in progress

